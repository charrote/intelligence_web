# 情报采集 Agent 规范

## 概述
情报采集由 **OpenClaw Agent** 通过 web_search/web_fetch 工具执行，替代原有的硬编码爬虫（engine.py）。

## 工作流程

### Step 1: 读取指挥指令
- 读取 `/home/uantek/dev/Applications/intelligence_web/data/scout_directives.md` 获取当前搜索方向
- 如果文件不存在或为空，使用 `scout_config.md` 中的默认关键词

### Step 2: 网络搜索
- 根据指挥指令中的关键词，使用 **web_search** 工具搜索
- 每个关键词搜索 3-5 条结果
- 目标源：Reddit、Tech Blogs、Industry News、Engineering Forums

### Step 3: 内容提取
- 对搜索结果中相关的 URL，使用 **web_fetch** 提取正文
- 提取标题、来源、摘要、原文链接

### Step 4: 写入 Inbox
- 将每条情报保存为 Markdown 文件到 `Inbox/` 目录
- 文件名格式：`{timestamp}_{slug_title}.md`
- 内容格式见下方模板

### Step 5: 录入系统
- 调用 API `POST http://localhost:8766/api/intelligence` 将情报录入数据库
- 状态自动为 `pending`（待审阅）

## Inbox 文件模板
```markdown
# {标题}

**来源:** {来源名称} ({URL})
**发现时间:** {YYYY-MM-DD HH:MM:SS}
**状态:** New

## 摘要
{正文摘要，300-500字}

---
*Generated by Intelligence_Web Scout Agent*
```

## 执行频率与分量控制
- **触发方式**: OpenClaw cron 定时任务（`intelligence-scout-hourly`）
- **执行频率**: 每小时整点一次（cron: `0 * * * *`，时区 Asia/Shanghai）
- **每次最多采集**: 20条（可通过 SCOUT_MAX_ITEMS 环境变量调整）
- **去重策略**: 
  - 对比 Inbox/ 目录中的文件名（模糊匹配）
  - 对比 API 数据库中的标题（精确匹配）
  - 同一天同一主题的搜索结果只保留最新的一条

## 定时任务配置
- **Job ID**: `8493e8e8-b1ba-4b47-986b-31c84aced345`
- **名称**: intelligence-scout-hourly
- **调度**: 每小时整点（Asia/Shanghai）
- **超时**: 600秒

## 注意事项
1. 每次执行前必须先读取指挥指令文件
2. 采集结果需要同时写入 Inbox 文件和 API 数据库
3. 达到数量上限后应立即停止，避免过度采集
4. 如果 API 不可用，记录错误日志但不中断流程
