# P0.1 — 数据库表结构与种子数据

> **状态：** ⏳ 待开始
> **前置条件：** 无（Phase 0 基础任务）
> **依赖本任务的后续任务：** P1.2, P1.3, P2.1, P2.2, P2.3, P3.1~P3.5

---

## 目标

在 `core/db.py` 的 `init_db()` 函数中新增 6 张表的 DDL 创建逻辑和种子数据灌入逻辑。

---

## 实施步骤

### Step 1：新增表 DDL

在 `init_db()` 函数中，在现有表（intelligence, history, commands, comments, summaries, settings, users）创建之后，新增以下 6 张表的 `CREATE TABLE IF NOT EXISTS` 语句：

#### 1.1 intel_extraction_rule

```sql
CREATE TABLE IF NOT EXISTS intel_extraction_rule (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    domain TEXT NOT NULL DEFAULT 'research',
    name TEXT NOT NULL UNIQUE,
    description TEXT DEFAULT '',
    scope TEXT NOT NULL DEFAULT 'full',
    max_fields INTEGER NOT NULL DEFAULT 15,
    enabled INTEGER NOT NULL DEFAULT 1,
    built_in INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
)
```

#### 1.2 intel_extraction_field

```sql
CREATE TABLE IF NOT EXISTS intel_extraction_field (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    rule_id INTEGER NOT NULL,
    field_key TEXT NOT NULL,
    field_label TEXT NOT NULL,
    field_type TEXT NOT NULL,
    is_required INTEGER NOT NULL DEFAULT 0,
    default_value TEXT DEFAULT '',
    sort_order INTEGER NOT NULL DEFAULT 0,
    help_text TEXT DEFAULT '',
    FOREIGN KEY (rule_id) REFERENCES intel_extraction_rule(id) ON DELETE CASCADE
)
```

#### 1.3 intel_fact

```sql
CREATE TABLE IF NOT EXISTS intel_fact (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    intel_id INTEGER NOT NULL,
    rule_id INTEGER NOT NULL,
    field_key TEXT NOT NULL,
    entity_name TEXT DEFAULT '',
    metric_name TEXT DEFAULT '',
    metric_value REAL,
    metric_unit TEXT DEFAULT '',
    time_period TEXT DEFAULT '',
    context TEXT DEFAULT '',
    source_anchor TEXT DEFAULT '',
    confidence TEXT NOT NULL DEFAULT 'high',
    created_at TEXT NOT NULL,
    FOREIGN KEY (intel_id) REFERENCES intelligence(id) ON DELETE CASCADE
)
```

#### 1.4 intel_aggregate

```sql
CREATE TABLE IF NOT EXISTS intel_aggregate (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    domain TEXT NOT NULL,
    name TEXT NOT NULL UNIQUE,
    description TEXT DEFAULT '',
    rule_id INTEGER NOT NULL,
    group_by TEXT NOT NULL,
    metrics TEXT DEFAULT '[]',
    filters TEXT DEFAULT '[]',
    chart_config TEXT DEFAULT '[]',
    prompt_template TEXT NOT NULL,
    schedule_minutes INTEGER NOT NULL DEFAULT 1440,
    lookback_days INTEGER NOT NULL DEFAULT 30,
    enabled INTEGER NOT NULL DEFAULT 1,
    next_run TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'active',
    fail_count INTEGER NOT NULL DEFAULT 0,
    last_fail_time TEXT,
    last_success_time TEXT,
    built_in INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    FOREIGN KEY (rule_id) REFERENCES intel_extraction_rule(id)
)
```

#### 1.5 report_run

```sql
CREATE TABLE IF NOT EXISTS report_run (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    template_id INTEGER NOT NULL,
    domain TEXT NOT NULL,
    scheduled_time TEXT NOT NULL,
    started_at TEXT,
    completed_at TEXT,
    status TEXT NOT NULL DEFAULT 'pending',
    aggregated_data TEXT,
    output_analysis TEXT,
    output_charts TEXT,
    output_summary TEXT,
    fact_count INTEGER,
    duration_sec INTEGER,
    error_msg TEXT,
    retry_count INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    FOREIGN KEY (template_id) REFERENCES intel_aggregate(id) ON DELETE CASCADE
)
```

#### 1.6 report_scheduler

```sql
CREATE TABLE IF NOT EXISTS report_scheduler (
    id INTEGER PRIMARY KEY DEFAULT 1,
    scheduler_enabled INTEGER NOT NULL DEFAULT 1,
    extract_enabled INTEGER NOT NULL DEFAULT 1,
    report_enabled INTEGER NOT NULL DEFAULT 1,
    extract_interval_min INTEGER NOT NULL DEFAULT 10,
    report_interval_min INTEGER NOT NULL DEFAULT 5,
    last_extract_time TEXT,
    last_report_time TEXT,
    extract_success_today INTEGER NOT NULL DEFAULT 0,
    report_success_today INTEGER NOT NULL DEFAULT 0,
    updated_at TEXT NOT NULL
)
```

---

### Step 2：新增索引

在表创建之后，为 `intel_fact` 添加以下索引：

```sql
CREATE INDEX IF NOT EXISTS idx_fact_intel ON intel_fact(intel_id);
CREATE INDEX IF NOT EXISTS idx_fact_rule ON intel_fact(rule_id);
CREATE INDEX IF NOT EXISTS idx_fact_field ON intel_fact(field_key);
CREATE INDEX IF NOT EXISTS idx_fact_entity ON intel_fact(entity_name);
CREATE INDEX IF NOT EXISTS idx_fact_time ON intel_fact(time_period);
```

---

### Step 3：种子数据灌入

在 `init_db()` 末尾（用户种子数据之后），新增内置规则、字段和报告模板的种子数据。

**判断逻辑：** 种子数据仅在表为空时灌入（使用 `SELECT COUNT(*)` 判断）。

#### 3.1 内置抽取规则（2 条）

```
规则1: name="厂商市场数据", domain="research", scope="full", max_fields=15, enabled=1, built_in=1
规则2: name="竞争情报摘要", domain="sales", scope="full+tables", max_fields=15, enabled=1, built_in=1
```

#### 3.2 内置抽取字段（规则1: 8 个字段）

| field_key | field_label | field_type | is_required | sort_order |
|-----------|-------------|------------|:-----------:|:----------:|
| company_name | 厂商名称 | company | 1 | 1 |
| market_share | 市场份额 | pct | 0 | 2 |
| market_size | 市场规模 | currency | 0 | 3 |
| currency | 币种 | currency_code | 0 | 4 |
| country | 国家/地区 | location | 0 | 5 |
| year | 年份 | year | 0 | 6 |
| growth_rate | 增长率 | pct | 0 | 7 |
| data_source | 数据来源 | text | 0 | 8 |

#### 3.3 内置抽取字段（规则2: 6 个字段）

| field_key | field_label | field_type | is_required | sort_order |
|-----------|-------------|------------|:-----------:|:----------:|
| competitor_name | 竞争对手名称 | company | 1 | 1 |
| action_type | 动作类型 | text | 1 | 2 |
| action_desc | 动作描述 | text | 0 | 3 |
| market_impact | 市场影响 | text | 0 | 4 |
| date | 事件日期 | date | 0 | 5 |
| source | 信息来源 | text | 0 | 6 |

#### 3.4 内置报告模板（4 条）

每条模板的 `prompt_template` 字段填充内置 Prompt 文本（见设计稿 §7.2）。

#### 3.5 report_scheduler 初始记录

```sql
INSERT OR IGNORE INTO report_scheduler(id, scheduler_enabled, extract_enabled,
    report_enabled, extract_interval_min, report_interval_min, updated_at)
VALUES (1, 1, 1, 1, 10, 5, now_isoformat())
```

---

### Step 4：intelligence 表新增 extracted 列

使用 `ALTER TABLE intelligence ADD COLUMN extracted INTEGER DEFAULT 0`，通过 migration 逻辑添加（参考 `core/db.py` 第 794-802 行的 migration 模式）。

---

## 代码位置

- **文件：** `core/db.py`
- **修改点：** `init_db()` 函数（约第 50-145 行区域）
- **migration 模式：** 参照第 794-802 行的现有 migration 模式

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|----------|
| 1 | 6 张新表均存在 | `SELECT name FROM sqlite_master WHERE type='table'` |
| 2 | `intel_fact` 有 5 个索引 | `PRAGMA index_list('intel_fact')` |
| 3 | 内置规则 2 条已灌入 | `SELECT COUNT(*) FROM intel_extraction_rule WHERE built_in=1` |
| 4 | 内置字段 14 条已灌入（规则1:8 + 规则2:6） | `SELECT COUNT(*) FROM intel_extraction_field` |
| 5 | 内置报告模板 4 条已灌入 | `SELECT COUNT(*) FROM intel_aggregate WHERE built_in=1` |
| 6 | `intelligence` 表有 `extracted` 列 | `PRAGMA table_info('intelligence')` |
| 7 | 数据库启动时无报错 | 启动容器/服务，查看日志 |

---

## 注意事项

1. **JSON 字段使用 TEXT 存储：** SQLite 没有原生 JSON 类型，使用 TEXT 存储 JSON 字符串，应用层用 `json.dumps()` / `json.loads()` 处理
2. **外键 ON DELETE CASCADE：** `intel_extraction_field` 在 `intel_extraction_rule` 删除时自动清理；`intel_fact` 在 `intelligence` 删除时自动清理；`report_run` 在 `intel_aggregate` 删除时自动清理
3. **report_scheduler 只保留 1 行：** `id DEFAULT 1` + `INSERT OR IGNORE` 保证唯一性
4. **next_run 初始值：** 报告模板的 `next_run` 设为 `created_at + schedule_minutes`
5. **不要修改现有表结构：** `intelligence` 表只在末尾 ADD COLUMN，不修改已有列

---

## 回滚方案

- 所有表可安全删除（`DROP TABLE IF EXISTS`），不影响已有 `intelligence` 数据
- `intelligence.extracted` 列可安全删除（`ALTER TABLE intelligence DROP COLUMN extracted`），不影响已有数据