# P2.3 — 报告生成流程实现

> **状态：** ⏳ 待开始
> **前置条件：** P1.3（SQL 聚合引擎）、P0.3（LLM 客户端）、P1.1（Prompt 渲染器）、P2.1（调度器框架）
> **依赖本任务的后续任务：** P3.5

---

## 目标

新建 `core/scheduler/report_engine.py`，实现"读取到期模板 → SQL 聚合 → 渲染 Prompt → LLM 文字总结 → 写入 report_run"的完整报告生成流程。

---

## 实施步骤

### Step 1：新建文件

在 `core/scheduler/` 目录下新建 `report_engine.py`。

### Step 2：实现核心函数

```python
"""Report engine: executes scheduled report generation."""
import json
import logging
from datetime import datetime, timezone

from core.db import get_db, get_setting
from core.scheduler.llm_client import call_llm, parse_json_from_response
from core.scheduler.prompt_renderer import render_report_prompt
from core.scheduler.sql_aggregator import aggregate

logger = logging.getLogger(__name__)


def run_scheduled_reports() -> dict:
    """
    执行所有到期的定时报告。

    Returns:
        {"executed": int, "success": int, "failed": int}
    """
    from core.db import get_db_path

    db_path = get_db_path()
    executed = 0
    success = 0
    failed = 0

    with get_db(db_path) as conn:
        templates = conn.execute(
            """SELECT * FROM intel_aggregate
               WHERE enabled = 1 AND next_run <= ? AND status != 'fused'
               ORDER BY next_run""",
            (datetime.now(timezone.utc).isoformat(),)
        ).fetchall()

        for tmpl in templates:
            template_dict = dict(tmpl)
            executed += 1

            result = run_single_report_internal(template_dict, conn)
            if result.get("ok"):
                success += 1
                _update_template_success(conn, template_dict["id"])
            else:
                failed += 1
                _handle_failure(conn, template_dict["id"], result.get("error"))

    return {"executed": executed, "success": success, "failed": failed}


def run_single_report_internal(template_dict: dict, conn) -> dict:
    """执行单个报告（内部调用，不重试）"""
    now = datetime.now(timezone.utc).isoformat()
    run_id = _create_run_record(conn, template_dict["id"], now)

    try:
        # Step 1: SQL 聚合
        agg_result = aggregate(template_dict)
        if agg_result.get("error"):
            _update_run_status(conn, run_id, "failed", error_msg=agg_result["error"])
            return {"ok": False, "error": f"Aggregation failed: {agg_result['error']}"}

        # Step 2: 构建图表数据
        chart_data = _build_charts(template_dict, agg_result["rows"])

        # Step 3: 渲染 Prompt
        system_prompt, user_prompt = render_report_prompt(
            report_name=template_dict["name"],
            start_date=_calc_start_date(template_dict["lookback_days"]),
            end_date=now,
            fact_count=agg_result["fact_count"],
            aggregated_data=json.dumps(agg_result["rows"], ensure_ascii=False),
            chart_data=json.dumps(chart_data, ensure_ascii=False),
            prompt_template=template_dict["prompt_template"],
        )

        # Step 4: LLM 文字总结
        timeout = int(get_setting("llm.report_timeout", "120"))
        llm_result = call_llm(
            system_prompt=system_prompt,
            user_prompt=user_prompt,
            temperature=0.1,
            max_tokens=1500,
            timeout=timeout,
        )

        if not llm_result.get("ok"):
            return {"ok": False, "error": llm_result.get("error")}

        parsed, json_ok = parse_json_from_response(llm_result["raw"])
        if not json_ok:
            return {"ok": False, "error": f"Report LLM JSON parse failed"}

        # Step 5: 写入 report_run
        _update_run_result(conn, run_id, {
            "aggregated_data": json.dumps(agg_result["rows"], ensure_ascii=False),
            "output_analysis": parsed.get("analysis", ""),
            "output_charts": json.dumps(chart_data, ensure_ascii=False),
            "output_summary": parsed.get("summary", ""),
            "fact_count": agg_result["fact_count"],
        })

        return {"ok": True}

    except Exception as e:
        logger.error(f"Report execution error template={template_dict['id']}: {e}")
        return {"ok": False, "error": str(e)}


def run_single_report(template_id: int) -> dict:
    """手动触发单个报告的执行（含重试）"""
    from core.db import get_db_path

    db_path = get_db_path()
    max_retries = int(get_setting("report.max_retries", "1"))
    retry_delay = int(get_setting("report.retry_delay_sec", "60"))

    with get_db(db_path) as conn:
        template = conn.execute(
            "SELECT * FROM intel_aggregate WHERE id = ?", (template_id,)
        ).fetchone()

        if not template:
            return {"ok": False, "error": "Template not found"}

        template_dict = dict(template)

        for attempt in range(1 + max_retries):
            result = run_single_report_internal(template_dict, conn)
            if result.get("ok"):
                return {"ok": True, "attempt": attempt + 1}

            if attempt < max_retries:
                import time
                time.sleep(retry_delay)

        return {"ok": False, "error": result.get("error"), "attempt": max_retries + 1}


# ─── 辅助函数 ─────────────────────────────────────────────

def _create_run_record(conn, template_id: int, scheduled_time: str) -> int:
    """创建 report_run 记录，返回 run_id"""
    cursor = conn.execute(
        """INSERT INTO report_run
           (template_id, scheduled_time, status, started_at, created_at)
           VALUES (?, ?, 'running', ?, ?)""",
        (template_id, scheduled_time, datetime.now(timezone.utc).isoformat(),
         datetime.now(timezone.utc).isoformat())
    )
    conn.commit()
    return cursor.lastrowid


def _update_run_status(conn, run_id: int, status: str, error_msg: str = None):
    """更新 run 状态"""
    now = datetime.now(timezone.utc).isoformat()
    params = [status, now, run_id]
    sql = f"UPDATE report_run SET status = ?, completed_at = ?, started_at = ? WHERE id = ?"
    if error_msg:
        sql = "UPDATE report_run SET status = ?, completed_at = ?, error_msg = ?, started_at = ? WHERE id = ?"
        params = [status, now, error_msg, run_id]

    conn.execute(sql, params)
    conn.commit()


def _update_run_result(conn, run_id: int, data: dict):
    """更新 run 结果为 completed"""
    now = datetime.now(timezone.utc).isoformat()
    conn.execute(
        """UPDATE report_run SET
           status = 'completed',
           completed_at = ?,
           aggregated_data = ?,
           output_analysis = ?,
           output_charts = ?,
           output_summary = ?,
           fact_count = ?,
           duration_sec = ?,
           started_at = ?
           WHERE id = ?""",
        (now, data.get("aggregated_data"), data.get("output_analysis"),
         data.get("output_charts"), data.get("output_summary"),
         data.get("fact_count"), 0, run_id)
    )
    conn.commit()


def _update_template_success(conn, template_id: int):
    """更新模板状态：成功执行"""
    now = datetime.now(timezone.utc).isoformat()
    conn.execute(
        """UPDATE intel_aggregate SET
           fail_count = 0,
           last_success_time = ?,
           next_run = datetime(?, '+{schedule_minutes} minutes')
           WHERE id = ?""",
        (now, now, template_id)
    )
    conn.commit()


def _handle_failure(conn, template_id: int, error_msg: str):
    """处理失败：增加 fail_count，检查是否熔断"""
    from core.db import get_setting

    now = datetime.now(timezone.utc).isoformat()
    fuse_threshold = int(get_setting("report.fuse_threshold", "3"))

    conn.execute(
        """UPDATE intel_aggregate SET
           fail_count = fail_count + 1,
           last_fail_time = ?
           WHERE id = ?""",
        (now, template_id)
    )
    conn.commit()

    row = conn.execute(
        "SELECT fail_count FROM intel_aggregate WHERE id = ?", (template_id,)
    ).fetchone()

    if row["fail_count"] >= fuse_threshold:
        conn.execute(
            "UPDATE intel_aggregate SET status = 'fused' WHERE id = ?",
            (template_id,)
        )
        conn.commit()
        logger.warning(f"Template {template_id} fused after {row['fail_count']} consecutive failures")


def _build_charts(template_dict: dict, rows: list) -> list:
    """根据 chart_config 将聚合数据转换为 ECharts 格式"""
    configs = json.loads(template_dict.get("chart_config", "[]"))
    charts = []

    for config in configs:
        chart_type = config.get("type", "bar")
        title = config.get("title", "图表")
        name_field = config.get("name_field", "entity_name")
        value_field = config.get("value_field", "metric_0")

        data = []
        for row in rows:
            item = {}
            if name_field in row:
                item["name"] = row[name_field]
            else:
                item["name"] = str(row.get("entity_name", ""))
            if value_field in row:
                item["value"] = row[value_field]
            else:
                item["value"] = 0
            data.append(item)

        charts.append({
            "type": chart_type,
            "title": title,
            "data": data
        })

    return charts


def _calc_start_date(lookback_days: int) -> str:
    """计算起始日期"""
    from datetime import timedelta
    start = datetime.now(timezone.utc) - timedelta(days=lookback_days)
    return start.strftime("%Y-%m-%d")


def _now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()
```

---

## 代码位置

- **文件：** `core/scheduler/report_engine.py`（新建）
- **依赖：** P1.1, P0.3, P1.3, P2.1

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|----------|
| 1 | `report_engine.py` 可导入 | `python -c "from core.scheduler.report_engine import run_scheduled_reports"` |
| 2 | 到期报告被执行 | 设置 `next_run` 为过去时间，验证 `report_run` 有新记录 |
| 3 | 聚合数据正确存入 `aggregated_data` | 检查 `report_run` 的 `aggregated_data` 字段 |
| 4 | LLM 文字分析正确存入 `output_analysis` | 检查 `report_run` 的 `output_analysis` 字段 |
| 5 | 图表数据正确存入 `output_charts` | 验证 JSON 格式含 ECharts 数据 |
| 6 | 执行成功后模板 `fail_count=0` | 验证 `intel_aggregate.fail_count` 归零 |
| 7 | 执行成功后 `next_run` 更新 | 验证 `next_run = now + schedule_minutes` |
| 8 | 执行失败后 `fail_count` 增加 | 模拟 LLM 失败，验证 `fail_count += 1` |
| 9 | 连续失败 3 次后标记 `fused` | 连续失败，验证 `status='fused'` |
| 10 | 熔断的模板不会被执行 | 验证 fused 模板不在到期列表中 |

---

## 注意事项

1. **`run_single_report_internal` 与 `run_single_report` 分离：** 内部函数不重试，外部函数负责重试逻辑，避免重复代码
2. **熔断检查在 `_handle_failure` 中：** 每次失败后自动检查，超过阈值自动标记 fused
3. **`_build_charts` 的 name_field 和 value_field 使用字符串索引：** 确保前端传参时字段名与 SQL 聚合输出的字段名一致
4. **duration_sec 当前固定为 0：** 后续可优化为记录实际执行耗时

---

## 回滚方案

- 删除 `core/scheduler/report_engine.py`