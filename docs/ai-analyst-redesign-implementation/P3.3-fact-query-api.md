# P3.3 — 事实查询 API

> **状态：** ⏳ 待开始
> **前置条件：** P0.1（数据库表结构）
> **依赖本任务的后续任务：** P4.4

---

## 目标

在 `core/app.py` 中新增 intel_fact 查询相关的 REST API 路由。

---

## 实施步骤

### Step 1：在 `core/app.py` 新增路由

```python
# ─── 抽取结果（结构化事实）查询 API ──────────────────────────

@app.route('/api/extract/facts', methods=['GET'])
@require_auth
def list_facts():
    """
    查询结构化事实。

    查询参数：
        rule_id:   按抽取规则过滤
        intel_id:  按情报 ID 过滤
        field_key: 按字段标识过滤
        entity:    按实体名称模糊匹配
        start_at:  开始时间
        end_at:    结束时间
        limit:     返回条数（默认 100，最大 1000）
        offset:    分页偏移
    """
    from core.db import get_db_path
    db_path = get_db_path()

    rule_id = request.args.get('rule_id')
    intel_id = request.args.get('intel_id')
    field_key = request.args.get('field_key')
    entity = request.args.get('entity')
    start_at = request.args.get('start_at')
    end_at = request.args.get('end_at')
    limit = min(int(request.args.get('limit', 100)), 1000)
    offset = int(request.args.get('offset', 0))

    with get_db(db_path) as conn:
        sql = "SELECT * FROM intel_fact WHERE 1=1"
        params = []

        if rule_id:
            sql += " AND rule_id = ?"
            params.append(rule_id)
        if intel_id:
            sql += " AND intel_id = ?"
            params.append(intel_id)
        if field_key:
            sql += " AND field_key = ?"
            params.append(field_key)
        if entity:
            sql += " AND (entity_name LIKE ? OR context LIKE ?)"
            params.append(f"%{entity}%", f"%{entity}%")
        if start_at:
            sql += " AND created_at >= ?"
            params.append(start_at)
        if end_at:
            sql += " AND created_at <= ?"
            params.append(end_at)

        sql += " ORDER BY created_at DESC"

        # 总数
        total = conn.execute(
            sql.replace("SELECT *", "SELECT COUNT(*)"), params
        ).fetchone()[0]

        # 分页
        rows = conn.execute(sql, params).fetchall()

        return jsonify({
            "total": total,
            "limit": limit,
            "offset": offset,
            "rows": [dict(r) for r in rows]
        })


@app.route('/api/extract/facts/<int:intel_id>', methods=['GET'])
@require_auth
def get_intel_facts(intel_id):
    """查询某条情报的所有抽取结果"""
    from core.db import get_db_path
    db_path = get_db_path()
    with get_db(db_path) as conn:
        rows = conn.execute(
            """SELECT f.*, r.name AS rule_name
               FROM intel_fact f
               LEFT JOIN intel_extraction_rule r ON f.rule_id = r.id
               WHERE f.intel_id = ?
               ORDER BY f.rule_id, f.field_key""",
            (intel_id,)
        ).fetchall()

        # 按规则分组
        grouped = {}
        for row in rows:
            rule_name = row["rule_name"] or f"rule_{row['rule_id']}"
            if rule_name not in grouped:
                grouped[rule_name] = []
            grouped[rule_name].append(dict(row))

        return jsonify({"intel_id": intel_id, "rules": grouped})


@app.route('/api/extract/stats', methods=['GET'])
@require_auth
def extract_stats():
    """抽取统计信息"""
    from core.db import get_db_path
    db_path = get_db_path()
    with get_db(db_path) as conn:
        total_intel = conn.execute(
            "SELECT COUNT(*) FROM intelligence"
        ).fetchone()[0]
        extracted = conn.execute(
            "SELECT COUNT(*) FROM intelligence WHERE extracted = 1"
        ).fetchone()[0]
        failed = conn.execute(
            "SELECT COUNT(*) FROM intelligence WHERE extracted = 2"
        ).fetchone()[0]
        pending = conn.execute(
            "SELECT COUNT(*) FROM intelligence WHERE extracted = 0"
        ).fetchone()[0]

        total_facts = conn.execute(
            "SELECT COUNT(*) FROM intel_fact"
        ).fetchone()[0]

        rules = conn.execute(
            "SELECT id, name, domain, enabled, "
            "(SELECT COUNT(*) FROM intel_fact WHERE rule_id=er.id) AS fact_count "
            "FROM intel_extraction_rule er ORDER BY name"
        ).fetchall()

        return jsonify({
            "total_intelligence": total_intel,
            "extracted": extracted,
            "extract_failed": failed,
            "pending_extract": pending,
            "total_facts": total_facts,
            "coverage_pct": round(extracted / total_intel * 100, 1) if total_intel > 0 else 0,
            "rules": [dict(r) for r in rules]
        })
```

---

## 代码位置

- **文件：** `core/app.py`

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|----------|
| 1 | `GET /api/extract/facts` 返回所有事实（分页） | 验证 `total` + `rows` |
| 2 | `?rule_id=1` 过滤正确 | 验证只返回 rule_id=1 的记录 |
| 3 | `?entity=Siemens` 模糊匹配 | 验证 entity_name 和 context 都被匹配 |
| 4 | `?limit=5&offset=5` 分页正确 | 验证 offset 偏移 |
| 5 | `GET /api/extract/facts/{intel_id}` 返回分组数据 | 验证 `rules` 对象结构 |
| 6 | `GET /api/extract/stats` 返回统计信息 | 验证 coverage_pct 计算正确 |

---

## 注意事项

1. **limit 上限 1000：** 防止单次查询返回过多数据导致 OOM
2. **模糊匹配使用 LIKE：** `entity` 参数同时匹配 `entity_name` 和 `context` 字段
3. **stats API 性能：** 统计查询使用多个简单 COUNT 查询，避免复杂 JOIN

---

## 回滚方案

- 删除本任务新增的路由代码段