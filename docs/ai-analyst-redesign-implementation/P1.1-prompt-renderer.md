# P1.1 — Jinja2 提示词渲染器

> **状态：** ⏳ 待开始
> **前置条件：** 无（Phase 1 基础任务，可与 P0.2/P0.3 并行）
> **依赖本任务的后续任务：** P1.2, P1.3, P2.2, P2.3

---

## 目标

新建 `core/scheduler/prompt_renderer.py`，封装 Jinja2 模板渲染，提供两个专用渲染函数：抽取 Prompt 渲染 + 报告分析 Prompt 渲染。

---

## 实施步骤

### Step 1：新建文件

在 `core/scheduler/` 目录下新建 `prompt_renderer.py`。

### Step 2：实现代码

```python
"""Jinja2 template renderer for extraction and report prompts."""
from jinja2 import Environment, StrictUndefined, BaseLoader

# 全局 Jinja2 环境（严格模式，未定义变量会报错）
_env = Environment(undefined=StrictUndefined)


def render(template_str: str, variables: dict) -> str:
    """
    通用 Jinja2 模板渲染函数。

    Args:
        template_str: Jinja2 模板字符串
        variables: 变量字典

    Returns:
        渲染后的字符串

    Raises:
        jinja2.UndefinedError: 模板中引用了未定义的变量
    """
    tmpl = _env.from_string(template_str)
    return tmpl.render(**variables)


def render_extraction_prompt(rule_name: str, fields: list,
                              intel_title: str, intel_content: str) -> str:
    """
    渲染情报结构化抽取 Prompt。

    Args:
        rule_name: 抽取规则名称
        fields: 字段列表，每项为 dict：{field_key, field_label, field_type, is_required}
        intel_title: 情报标题
        intel_content: 情报内容

    Returns:
        完整的抽取 Prompt 字符串（包含系统提示 + 用户提示）
    """
    system_prompt = """你是一个专业的情报数据提取员。你的任务是从文本中提取指定字段的数据。

重要规则：
1. 只提取文本中明确提到的信息，不要推断或虚构
2. 找不到的字段填 null
3. 严格按 JSON 格式返回，不要包含任何其他内容
4. 数值字段请提取为数字（不是字符串）
5. 如果文本中提到多个实体（如多家公司），只提取第一个出现的完整记录"""

    fields_list = ""
    for f in fields:
        req = "，必填" if f.get("is_required") else ""
        fields_list += f'- {f["field_key"]}（{f["field_label"]}）：类型为 {f["field_type"]}{req}\n'

    json_fields = ",\n  ".join(f'"{f["field_key"]}": null' for f in fields)

    user_prompt = f"""请从以下情报文本中提取以下字段的数据：

【抽取规则】{rule_name}

【字段定义】
{fields_list}
【情报标题】{intel_title}

【情报内容】
{intel_content}

请严格按以下 JSON 格式返回（字段顺序与定义一致）：
{{
  {json_fields}
}}"""

    return system_prompt, user_prompt


def render_report_prompt(report_name: str, start_date: str, end_date: str,
                          fact_count: int, aggregated_data: str,
                          chart_data: str, prompt_template: str) -> str:
    """
    渲染报告分析 Prompt。

    Args:
        report_name: 报告名称
        start_date / end_date: 数据范围
        fact_count: 参与聚合的事实条数
        aggregated_data: JSON 格式的聚合数据
        chart_data: JSON 格式的图表数据
        prompt_template: 用户自定义的 prompt_template（Jinja2 格式）

    Returns:
        (system_prompt, user_prompt)

    注意：如果 prompt_template 包含 Jinja2 变量，使用全局 render() 渲染。
    系统 Prompt 固定为分析报告角色。
    """
    system_prompt = """你是专业情报分析师。请基于已聚合的数据，撰写市场分析报告。

重要规则：
1. 只描述已有数据，不要推断或虚构任何信息
2. 所有数据来自 JSON 格式提供的聚合结果
3. 报告应有逻辑结构，先总后分
4. 语言简洁专业
5. 严格 JSON 格式返回"""

    variables = {
        "report_name": report_name,
        "start_date": start_date,
        "end_date": end_date,
        "fact_count": fact_count,
        "aggregated_data": aggregated_data,
        "chart_data": chart_data,
    }

    try:
        user_prompt = render(prompt_template, variables)
    except Exception:
        # 模板渲染失败时使用默认模板
        user_prompt = f"""【报告名称】{report_name}
【分析范围】{start_date} 至 {end_date}
【参与分析的数据】{fact_count} 条结构化事实

=== 数据聚合结果 ===
{aggregated_data}

=== 图表数据 ===
{chart_data}

请基于以上数据撰写分析报告，按 JSON 格式返回：
{{
  "analysis": "文字分析内容...",
  "summary": "一段话总结..."
}}"""

    return system_prompt, user_prompt
```

---

## 代码位置

- **文件：** `core/scheduler/prompt_renderer.py`（新建）
- **依赖：** P0.2（jinja2 依赖已安装）

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|----------|
| 1 | `prompt_renderer.py` 存在且可导入 | `python -c "from core.scheduler.prompt_renderer import render"` |
| 2 | `render_extraction_prompt()` 正确生成抽取 Prompt | 断言系统 Prompt 包含"数据提取员"，用户 Prompt 包含字段定义 |
| 3 | `render_report_prompt()` 正确渲染自定义模板 | 传入含 `{{ report_name }}` 的模板，验证渲染结果 |
| 4 | 未定义变量时抛出 UndefinedError | 传入缺变量的模板，验证异常 |
| 5 | 默认模板 fallback | 传入无效模板字符串，验证使用默认模板 |

---

## 注意事项

1. **使用 StrictUndefined** — 模板中引用了未定义的变量时直接报错，避免静默使用空值
2. **系统 Prompt 固定，用户 Prompt 可配置** — 用户只能通过 prompt_template 自定义分析部分的指令，系统角色不可修改
3. **JSON 变量名用花括号转义** — Jinja2 的 `{{ }}` 与 JSON 的 `{}` 冲突，在 `render_extraction_prompt` 中 JSON 模板的字段用 `{{"key": null}}` 转义

---

## 回滚方案

- 删除 `core/scheduler/prompt_renderer.py`