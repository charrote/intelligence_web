# P0.3 — LLM 客户端封装

> **状态：** ⏳ 待开始
> **前置条件：** P0.2（依赖已安装）
> **依赖本任务的后续任务：** P1.2, P1.3

---

## 目标

新建 `scheduler/llm_client.py`，封装对 OpenAI 兼容 API 和 Anthropic API 的 HTTP 调用，供抽取引擎和报告引擎复用。

---

## 实施步骤

### Step 1：新建文件

在 `core/` 目录下新建 `scheduler/` 文件夹（若不存在），创建 `llm_client.py`。

### Step 2：实现核心函数

```python
"""LLM API client for extraction and report generation tasks."""
import json
import requests
from core.db import get_setting


# ─── 配置读取 ──────────────────────────────────────────────

def get_llm_config():
    """读取 LLM 配置（从 settings 表）"""
    config = {
        "provider": get_setting("model.provider", "openai"),
        "api_base_url": get_setting("model.api_base_url", "https://api.openai.com"),
        "api_key": get_setting("model.api_key", ""),
        "model_name": get_setting("model.name", "gpt-4"),
    }
    return config


# ─── OpenAI 兼容 API ───────────────────────────────────────

def call_openai_compatible(system_prompt: str, user_prompt: str,
                           temperature: float = 0.1, max_tokens: int = 500,
                           timeout: int = 60) -> dict:
    """调用 OpenAI 兼容 API"""
    config = get_llm_config()
    response = requests.post(
        url=f"{config['api_base_url'].rstrip('/')}/v1/chat/completions",
        headers={
            "Authorization": f"Bearer {config['api_key']}",
            "Content-Type": "application/json"
        },
        json={
            "model": config["model_name"],
            "messages": [
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_prompt}
            ],
            "temperature": temperature,
            "max_tokens": max_tokens,
        },
        timeout=timeout
    )
    response.raise_for_status()
    data = response.json()
    content = data["choices"][0]["message"]["content"]
    return {"raw": content, "ok": True}


# ─── Anthropic API ─────────────────────────────────────────

def call_anthropic(system_prompt: str, user_prompt: str,
                   temperature: float = 0.1, max_tokens: int = 500,
                   timeout: int = 60) -> dict:
    """调用 Anthropic Messages API"""
    config = get_llm_config()
    response = requests.post(
        url=f"{config['api_base_url'].rstrip('/')}/v1/messages",
        headers={
            "x-api-key": config["api_key"],
            "anthropic-version": "2023-06-01",
            "Content-Type": "application/json"
        },
        json={
            "model": config["model_name"],
            "system": system_prompt,
            "messages": [{"role": "user", "content": user_prompt}],
            "temperature": temperature,
            "max_tokens": max_tokens,
        },
        timeout=timeout
    )
    response.raise_for_status()
    data = response.json()
    content = data["content"][0]["text"]
    return {"raw": content, "ok": True}


# ─── 统一入口 ──────────────────────────────────────────────

def call_llm(system_prompt: str, user_prompt: str,
             temperature: float = 0.1, max_tokens: int = 500,
             timeout: int = 60) -> dict:
    """
    统一 LLM 调用入口。根据配置自动选择 API 提供商。

    Returns:
        {"raw": str, "ok": True}   成功
        {"error": str, "ok": False} 失败（包含异常信息）
    """
    try:
        config = get_llm_config()
        if config["provider"] == "anthropic":
            return call_anthropic(system_prompt, user_prompt,
                                  temperature, max_tokens, timeout)
        else:
            return call_openai_compatible(system_prompt, user_prompt,
                                          temperature, max_tokens, timeout)
    except requests.exceptions.Timeout:
        return {"error": f"LLM API timeout ({timeout}s)", "ok": False}
    except requests.exceptions.ConnectionError:
        return {"error": f"LLM API connection failed: {config.get('api_base_url', '')}", "ok": False}
    except requests.exceptions.HTTPError as e:
        return {"error": f"LLM API HTTP error: {e.response.status_code} {e.response.text[:200]}", "ok": False}
    except Exception as e:
        return {"error": f"LLM call error: {str(e)[:200]}", "ok": False}


# ─── JSON 响应解析 ─────────────────────────────────────────

def parse_json_from_response(raw: str) -> tuple:
    """
    从 LLM 原始响应中提取 JSON 对象。

    支持以下情况：
    - 纯 JSON
    - ```json ... ``` 包裹
    - 其他 ```...``` 包裹
    - 带前后文字的 JSON

    Returns:
        (parsed_dict, True)   成功
        (None, False)         解析失败
    """
    raw = raw.strip()

    # 尝试直接解析
    try:
        return json.loads(raw), True
    except json.JSONDecodeError:
        pass

    # 尝试提取 ```json ... ``` 块
    import re
    match = re.search(r'```(?:json)?\s*(\{.*?\})\s*```', raw, re.DOTALL)
    if match:
        try:
            return json.loads(match.group(1)), True
        except json.JSONDecodeError:
            pass

    # 尝试提取 {{ ... }}
    match = re.search(r'\{(.*)\}', raw, re.DOTALL)
    if match:
        try:
            return json.loads("{" + match.group(1) + "}"), True
        except json.JSONDecodeError:
            pass

    return None, False
```

---

## 代码位置

- **文件：** `core/scheduler/llm_client.py`（新建）
- **目录结构：**
```
core/
├── scheduler/
│   ├── __init__.py      ← 如果目录不存在，新建一个空的 __init__.py
│   ├── llm_client.py    ← 新建，本任务
│   ├── prompt_renderer.py  ← P1.1
│   ├── extraction_engine.py  ← P1.2
│   ├── sql_aggregator.py     ← P1.3
│   └── scheduler.py          ← P2.1
```

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|----------|
| 1 | `llm_client.py` 存在且可导入 | `python -c "from core.scheduler.llm_client import call_llm"` |
| 2 | `get_llm_config()` 返回预期配置 | 设置模型参数后验证返回值 |
| 3 | `call_openai_compatible()` 正常调用 | 使用测试 API key 验证（可用 mock） |
| 4 | `call_anthropic()` 正常调用 | 使用 Anthropic API key 验证 |
| 5 | `call_llm()` 异常时返回 `{"ok": False}` | 模拟网络错误验证 |
| 6 | `parse_json_from_response()` 支持 3 种格式 | 测试纯 JSON / ```json 包裹 / 带文字 |

---

## 注意事项

1. **不要在此模块修改 settings 数据** — 只读配置，不写配置
2. **超时异常要区分类型** — Timeout 和 ConnectionError 返回不同错误信息，便于调度器做不同处理
3. **HTTPError 时截断响应体** — 只保留前 200 字符，避免日志过长
4. **JSON 解析支持 Markdown 代码块** — LLM 经常返回 ```json {...} ``` 格式

---

## 回滚方案

- 删除 `core/scheduler/` 目录
- 恢复 requirements.txt