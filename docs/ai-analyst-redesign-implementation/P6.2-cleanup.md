# P6.2 — 收尾清理

| 项目 | 值 |
|------|-----|
| 任务目录 | `docs/ai-analyst-redesign-implementation/` |
| 设计文档 | `docs/04.ai-analyst-redesign.md` |
| 状态 | ⏳ 待开始 |
| 前置条件 | P6.1（全部验收通过） |
| 依赖本任务的后续任务 | 无（项目完成） |

---

## 目标

在全部功能验收通过后，进行收尾清理工作。

---

## 实施步骤

### Step 1：清理旧代码

| 文件/代码 | 操作 | 说明 |
|-----------|------|------|
| `core/app.py` 中 `analyst_analyze()` 路由 | 加 `# Deprecated` 注释 | 保留兼容，不删除 |
| `portal/analyst.html` 旧版实时问答代码 | 已替换（P4.5） | 确认无残留 |
| `docs/04.ai-analyst-redesign.md` | 从 docs 根目录移到 `docs/04.ai-analyst-redesign/` | 作为设计文档归档 |

### Step 2：更新项目文档

| 文件 | 操作 |
|------|------|
| `docs/01.architecture.md` | 更新架构图，反映新的四层数据架构 |
| `docs/02.product-introduction.md` | 更新产品介绍，反映 AI 分析师功能变更 |
| `README.md` | 更新项目说明，提及新模块 |

### Step 3：更新 `core/domains.json`

确认研究域和销售域的 domain_spec 中无需修改（新功能在核心层实现，不影响域配置）。

### Step 4：git commit

```bash
# 提交所有变更
git add core/ portal/ scheduler/ docker-compose.yml requirements.txt docs/
git commit -m "feat: AI分析师重构为定时报告系统

- 新增情报结构化抽取引擎（AI 离线抽取）
- 新增 intel_fact 表（结构化事实存储）
- 新增抽取规则 + 报告模板管理
- 新增定时报告调度器（APScheduler）
- 前端新增抽取规则、报告模板、查看报告、快速执行四个页面
- 移除实时问答，改为离线定时报告模式"
```

### Step 5：更新 CHANGELOG

在项目 CHANGELOG 中记录本次重构：

```
## [vX.Y.0] - 2026-08-11

### Added
- AI 分析师重构为定时报告系统
- 情报结构化抽取（AI 离线抽取 + intel_fact 表）
- 抽取规则管理（前端 + API）
- 报告模板管理（前端 + API）
- 定时报告调度器
- 报告查看与历史版本

### Changed
- AI 分析师从实时问答改为离线定时报告
- 报告数据从 LLM 实时推理改为 SQL 聚合 + LLM 文字总结

### Removed
- 实时问答 API（analyst_analyze）前端不再调用
```

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|----------|
| 1 | 无残留旧代码 | 检查 `analyst_analyze` 路由无引用 |
| 2 | 项目文档已更新 | 检查 architecture.md / product-intro.md |
| 3 | git commit 成功 | `git log --oneline -1` |
| 4 | CHANGELOG 已更新 | 检查 CHANGELOG |
| 5 | docker-compose.yml 正确启动 | `docker-compose up -d` |

---

## 注意事项

1. **不要删除旧代码，只加注释：** `analyst_analyze` 保留但不暴露，作为兼容接口
2. **CHANGELOG 使用 Keep a Changelog 格式：** Added / Changed / Removed
3. **确认 domains.json 无需修改：** 新功能在核心层实现，不影响域配置

---

## 回滚方案

- `git revert HEAD` 回滚所有变更