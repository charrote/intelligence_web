# P1.2 — 情报结构化抽取引擎

> **状态：** ⏳ 待开始
> **前置条件：** P0.1（数据库表 + intelligence.extracted 列）、P0.3（LLM 客户端）、P1.1（Prompt 渲染器）
> **依赖本任务的后续任务：** P2.2

---

## 目标

新建 `core/scheduler/extraction_engine.py`，实现"读取未抽取情报 → 按规则构造 Prompt → 调用 LLM → 解析 JSON → 写入 intel_fact 表"的完整抽取流程。

---

## 实施步骤

### Step 1：新建文件

在 `core/scheduler/` 目录下新建 `extraction_engine.py`。

### Step 2：实现核心函数

```python
"""Extraction engine: extracts structured facts from intelligence records."""
import json
import logging
from datetime import datetime, timezone

from core.db import get_db, get_setting
from core.scheduler.llm_client import call_llm, parse_json_from_response
from core.scheduler.prompt_renderer import render_extraction_prompt

logger = logging.getLogger(__name__)


def extract_all_pending() -> dict:
    """
    抽取所有未抽取的新情报。

    Returns:
        {"processed": int, "success": int, "failed": int}
    """
    from core.db import get_db_path

    db_path = get_db_path()
    processed = 0
    success = 0
    failed = 0

    with get_db(db_path) as conn:
        pending = conn.execute(
            "SELECT id, title, content FROM intelligence WHERE extracted = 0"
        ).fetchall()

        if not pending:
            return {"processed": 0, "success": 0, "failed": 0}

        rules = conn.execute(
            "SELECT id, name FROM intel_extraction_rule WHERE enabled = 1"
        ).fetchall()

        for intel_row in pending:
            intel_id = intel_row["id"]
            intel_title = intel_row["title"]
            intel_content = intel_row["content"]
            row_success = True

            for rule_row in rules:
                rule_id = rule_row["id"]
                rule_name = rule_row["name"]
                fields = _get_rule_fields(conn, rule_id)
                result = _extract_single(intel_id, rule_id, rule_name,
                                          fields, intel_title, intel_content, conn)
                if not result["ok"]:
                    row_success = False
                    logger.warning(f"Extract failed intel={intel_id} rule={rule_id}: {result.get('error')}")

            # 更新情报状态
            new_status = 1 if row_success else 2  # 1=成功, 2=部分失败
            conn.execute(
                "UPDATE intelligence SET extracted = ? WHERE id = ?",
                (new_status, intel_id)
            )
            conn.commit()

            if row_success:
                success += 1
            else:
                failed += 1
            processed += 1

    return {"processed": processed, "success": success, "failed": failed}


def _get_rule_fields(conn, rule_id: int) -> list:
    """读取规则的字段列表"""
    rows = conn.execute(
        "SELECT * FROM intel_extraction_field WHERE rule_id = ? ORDER BY sort_order",
        (rule_id,)
    ).fetchall()
    return [dict(r) for r in rows]


def _extract_single(intel_id: int, rule_id: int, rule_name: str,
                     fields: list, intel_title: str, intel_content: str,
                     conn) -> dict:
    """
    对单条情报 × 单条规则执行抽取。

    Returns:
        {"ok": True, "fact_count": int}  成功
        {"ok": False, "error": str}       失败
    """
    timeout = int(get_setting("llm.extract_timeout", "60"))

    system_prompt, user_prompt = render_extraction_prompt(
        rule_name, fields, intel_title, intel_content
    )

    result = call_llm(
        system_prompt=system_prompt,
        user_prompt=user_prompt,
        temperature=0.1,
        max_tokens=500,
        timeout=timeout
    )

    if not result.get("ok"):
        return {"ok": False, "error": result.get("error", "LLM call failed")}

    parsed, json_ok = parse_json_from_response(result["raw"])
    if not json_ok:
        return {"ok": False, "error": f"JSON parse failed. Raw: {result['raw'][:200]}"}

    fact_count = _save_facts(conn, intel_id, rule_id, fields, parsed)
    return {"ok": True, "fact_count": fact_count}


def _save_facts(conn, intel_id: int, rule_id: int, fields: list,
                 data: dict) -> int:
    """
    将 LLM 返回的 JSON 数据写入 intel_fact 表。

    Returns:
        写入的事实条数
    """
    now = datetime.now(timezone.utc).isoformat()
    count = 0

    for field in fields:
        fk = field["field_key"]
        value = data.get(fk)
        if value is None:
            continue

        # 根据字段类型决定写入哪个列
        entity_name = ""
        metric_name = ""
        metric_value = None
        metric_unit = ""
        time_period = ""
        context = ""

        ft = field["field_type"]
        if ft == "company":
            entity_name = str(value)
        elif ft in ("pct", "number"):
            metric_name = field["field_label"]
            metric_value = _safe_float(value)
            if ft == "pct":
                metric_unit = "%"
        elif ft == "currency":
            metric_name = field["field_label"]
            metric_value = _safe_float(value)
        elif ft == "currency_code":
            metric_unit = str(value)
        elif ft == "location":
            entity_name = str(value)
        elif ft == "year":
            metric_name = "年份"
            metric_value = _safe_float(value)
            metric_unit = "年"
        elif ft == "date":
            time_period = str(value)
        elif ft == "text":
            context = str(value)

        conn.execute(
            """INSERT INTO intel_fact
               (intel_id, rule_id, field_key, entity_name, metric_name,
                metric_value, metric_unit, time_period, context, confidence, created_at)
               VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)""",
            (intel_id, rule_id, fk, entity_name, metric_name,
             metric_value, metric_unit, time_period, context, "high", now)
        )
        count += 1

    return count


def _safe_float(val) -> float:
    """安全转换为 float，失败返回 0.0"""
    try:
        return float(str(val).replace(",", "").replace("，", ""))
    except (ValueError, TypeError):
        return 0.0
```

---

## 代码位置

- **文件：** `core/scheduler/extraction_engine.py`（新建）
- **依赖模块：** `core.db`, `core.scheduler.llm_client`, `core.scheduler.prompt_renderer`

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|----------|
| 1 | `extraction_engine.py` 可导入 | `python -c "from core.scheduler.extraction_engine import extract_all_pending"` |
| 2 | 新情报（extracted=0）被正确识别 | 插入 test 情报后验证 pending 列表 |
| 3 | 抽取成功后写入 intel_fact | 验证 `SELECT * FROM intel_fact WHERE intel_id=?` |
| 4 | 字段类型映射正确 | 测试 company/pct/currency/location/year/date/text 7 种类型 |
| 5 | LLM 返回 JSON 解析成功 | 模拟 LLM 返回 JSON，验证 `parse_json_from_response` |
| 6 | LLM 失败时返回 `{"ok": False}` | 模拟 LLM 超时/错误 |
| 7 | 抽取失败后 `extracted=2` | 模拟抽取失败，验证 intelligence 表状态 |
| 8 | 抽取成功后 `extracted=1` | 验证 intelligence 表状态 |

---

## 注意事项

1. **extracted 状态含义：**
   - `0` = 未抽取
   - `1` = 抽取成功
   - `2` = 抽取部分失败（某些规则失败，但其他规则成功）
   - 调度器只处理 `extracted = 0` 的记录，`2` 的记录下次不会重试（已尽力）

2. **field_type 映射：** 字段类型决定数据写入 `intel_fact` 的哪一列，务必确保映射逻辑覆盖所有字段类型

3. **LLM 超时参数从 settings 读取：** `llm.extract_timeout`，默认 60 秒，用户可在设置页修改

---

## 回滚方案

- 删除 `core/scheduler/extraction_engine.py`
- 清理 `intel_fact` 表中的测试数据（不影响其他表）